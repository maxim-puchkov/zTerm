# Function options
## -x :: Option with default value
## -y :: Option
local -A opts
opts=( [-x]='default value' )
local -a specs
specs=( ${(k)^opts#-}':' )
if [[ $# -gt 0 ]] &&
   {! zparseopts -D -K -M -A opts - $specs}; then
  return 1
fi


