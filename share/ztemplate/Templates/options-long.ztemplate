# Function options
# -x, --xlong: Option with default value
# -y, --ylong: Option
local -A opts
opts=( [-x]='default value' )
local -a {alt,}specs
specs=('x:' 'y:')
altspecs=('-xlong:=x' '-ylong:=y')
if [[ $# -gt 0 ]] &&
   {! zparseopts -D -K -M -A opts - $specs $altspecs}; then
  return 1
fi


