#compdef pwd


_arguments -S \
    '(-P):-L[display the logical current working directory (default)]' \
    '(-L):-P[display the physical current working directory (all symbolic links resolved)]' \
    '*:: :->' && return 0
