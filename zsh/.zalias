# User-specific profile for interactive zsh(1) shells


#MARK: - Global Aliases
# Shell output
alias -g noerr='2>/dev/null'  # redirect stderr
alias -g 2NIL='2>/dev/null'
alias -g quiet='&>/dev/null'  # redirect stdout and stderr
alias -g NIL='&>/dev/null'

# 'if [[ ... ]] _T'
alias -g _T='; then print -P -- $TRUE; else print -P -- $FALSE; fi;'
# 'for x in $array _F', 'while read x _F'
alias -g _F='; do print-var x; done;'



#MARK: - Regular Aliases
# Command 'ls'
alias ls='command ls -BFGHhk'
alias la='ls -la'
alias l='ls -l'
alias ll='ls -la'
alias lll='ls -la'
alias llll='ls -laR'

# Command 'chflags'
alias lock='command chflags uchg'
alias unlock='command chflags nouchg'

# Command 'cp'
alias cp='/bin/cp -i'

# Builtin 'echo'
alias e='builtin echo -'
alias E='() ( print -P -- "%K{1}%F{15}%B${argv}%b%f%k"; )'

# Builtin 'functions'
alias fns='functions'

# Builtin 'print'
#alias print='builtin print -P'
alias printp='builtin print -P'
alias printl='builtin print -l'
alias printn='builtin print -n'
alias printd='builtin print -P -D'
alias p='print'
alias pp='printp'
alias pl='printl'
alias pn='printn'

alias pyg='pygmentize'

# Builtin 'exit'
alias q='builtin exit'


# Command 'git'
alias gs='git status'
alias grs='git restore --staged'
alias gt='() { cd "$zterm" && git status; };'

# Command 'grep'
alias gr='grep'
alias grepa='egrep --color=always --text --regexp ".*"'
alias mang='grepman'

# Help
alias {h,help}='run-help'

# Command 'launchctl'
alias lctl='command launchctl'

# Command 'mv'
alias mv='/bin/mv -i'

# Command 'open'
alias o='open "$PWD"'
alias xopen='open -a Xcode'
alias textopen='open -a TextEdit'
alias webopen='open -a Safari'
alias scriptopen='open -a "Script Editor"'
alias vb='open -a VirtualBox'

# Command 'openssl'
alias sha256='openssl sha256'

# Builtin 'set'
alias set1='set -- -a -b -c'
alias set2='set -- -a -b -c -d D -e E -f F'
alias set3='set -- -a -b -c -d D -e E -f F --long LONG args "hello world"'

# Python
alias python='command python3'
alias python3.8='/usr/local/opt/python@3.8/bin/python3.8'
alias pip='command pip3'
alias idle='command idle3'

# Command 'rm'
alias rm='/bin/rm -i'

# Command 'strings'
alias str='command strings'

# Builtin 'whence'
alias wh='builtin whence -av'




#MARK: - Extended Commands
alias cd='cdp'
#alias ln='lnp'


alias ts='typeset'

function Array array() {
    typeset type=${0[1]}
    typeset id=$1
    shift
    if [[ -v $id ]]; then
        unset $id
    fi
    typeset $type -g $id
    set -A $id $argv
    if [[ -n ${(P)id} ]]; then
        notify -P -- "%F{4}%U${id}%u%f[${#}] = "\
            "{ ${(j:, :)argv} }" >&2
    fi
}
#function Array() {
#    typeset id=$1
#}

#MARK: - Custom Commands
# List
alias ltc='list tc'
alias lta='list ta'
alias ltch='list tch'
alias ltm='list tm'
alias lS='list S'
alias lSa='list -a S'
alias lf='list f'
alias limg='imgls'

# Brew
alias bb='brew_browse'
alias bi='brew info'

# 'cd'
alias cfd='cd -- "$(pfd)"'

# Print
alias pv='print-var'
alias pv@='print-var @'
alias pvo='print-var @ optlist'
alias pvt='print-var -t'

alias pfs='print-fstack'
alias pdb='print-debug'

# Show command source
alias ss='showsrc'

# Type
alias i='infocmd'
#alias t='print-var -t'



alias rre='open -a Xcode "$DEV/bin/run.sh"'
alias rr='zsh "$DEV/bin/run.sh"'

alias rte='open -a Xcode $DEV/bin/run-tests.zsh'
alias rt='zsh "$DEV/bin/run-tests.zsh"'


alias my-phone='print -P "%F{14}+1 %B(778) 512-7614‬%b%f"'
alias my-postal='print -P "%F{14}V3T 2S5%f"'


alias C='retc'


function print-control-char() {
    typeset -i10 -Z2 code
    for code in $argv; do
        typeset hex=0x${code[1,2]}
        if [[ -t 1 ]]; then
            printf '%s:  ' "${hex}"
        fi
        printf '%b\n' "\\${hex}"
    done
    return 0
}
