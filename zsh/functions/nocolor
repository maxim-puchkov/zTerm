#!/bin/zsh

#  nocolor
#  zTerm (id: 60)
#
#  Generated by 'zfn' on 2020-10-24
#  Copyright © 2020 Maxim Puchkov. All rights reserved.


# nocolor
#
# <#Description...#>
#
# Usage:
# nocolor
# <#usage...#>
emulate -L zsh
setopt extended_glob

if [[ ! -t 0 ]] ||
   [[ -p /dev/stdin ]]; then
  set -- ${argv} ${(@f)"$(<&0)"}
fi

printf '%s\n' "${*//$'\033['[0-9;]#m/}"
