#!/bin/zsh

#  synopsis
#  /usr/local/zterm/zsh/functions (id: 36)
#
#  Generated by 'ztemplate_apply' (zterm/zsh/sources) on 2020-10-12
#  Copyright © 2020 Maxim Puchkov. All rights reserved.


# synopsis:
# <#Function description#>
#
# Usage:
# synopsis  <#...#>
emulate -LR zsh
autoload -Uz error

if (command man -W $argv &>/dev/null); then
  command man $argv \
  | command col -b \
  | command awk '/^SYN/, /^$/ {print $0;}' \
  | command sed -e '/^SYN/d' -e '/^$/d' \
  | command cat -n
else
  error -1 'no manual page for ${argv}'
fi
