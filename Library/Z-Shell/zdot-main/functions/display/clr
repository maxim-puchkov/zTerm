#!/bin/zsh

#  clr
#  zTerm (id: 61)
#
#  Generated by 'zfn' on 2020-10-24
#  Copyright © 2020 Maxim Puchkov. All rights reserved.


# clr
#
# Clear terminal buffer.
#
# Usage:
# clr
emulate -LR zsh


local -A opts
local -a specs
specs=('r' 's')
if [[ $# -gt 0 ]] &&
   {! zparseopts -D -K -M -A opts - $specs}; then
  return 1
fi


local -a esc_seq=('\e[H' '\e[2J')

# Also clear scrollback buffer.
if [[ $+opts[-s] -eq 1 ]]; then
  
fi



# Clear buffer.
printf '%b%b%b' '\e[H' '\e[2J' '\e[3J'


# @option -r
# @description Reset shell.
if [[ $+opts[-r] -eq 1 ]]; then
  trap '
    print -P -- "[%*] %F{4}Reset ${0#-}%f"
    builtin exec -l ${0#-}
  ' EXIT
fi
