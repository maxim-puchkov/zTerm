#compdef readf

# _readf
# /usr/local/Terminal/zsh/site-functions/readf
#
# zsh function completion
# Updated on 2020-06-24 at 06:52 pm


_arguments -C \
      -e"[exclude lines matching pattern]" \
      -s"[skip empty lines]" \
  ":command:->command" \
  "*::arg:->arg"

case $state in
  (command)
    typeset -a commands
    commands=(
      v1:"''"
    )
    _describe -t commands command commands
  ;;
  (arg)
    typeset -a args
    args=(
      # Completion for commands' arguments
    )
    _describe -t args arg args
  ;;
esac

return 0
